{
  "name": "sailer-2d-felice",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:host": "vite --host",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && vite build",
    "test": "vitest run",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.10",
    "vitest": "^4.0.7"
  },
  "dependencies": {
    "pixi.js": "^8.18.1"
  }
}
